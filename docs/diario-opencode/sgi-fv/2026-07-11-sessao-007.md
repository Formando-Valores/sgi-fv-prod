# Sessão 007 — 2026-07-11

## Commits

| Commit | Descrição |
|--------|-----------|
| `689d998` | fix: hide password rules by default, show on focus/blur nos formulários externos |

## Alterações

### 1. Wix/external forms: password rules visibility UX
- **Arquivos:** `docs/integrations/wix-client-intake.html`, `docs/integrations/external-client-intake.html`
- Regras de senha (`#passwordRules`) começam ocultas (`display:none`)
- Mostram ao focar no campo de senha, escondem ao blur (se vazio)
- Mantêm visíveis enquanto o campo tem valor digitado

### 2. Repositório limpo
- 85 arquivos não relacionados removidos do tracking (cardapio-inteligente, palpiteiros-novo, etc.)
- `.gitignore` atualizado com padrões `.env`, `.next/`, `__pycache__/`, `tsconfig.tsbuildinfo`
- Riscos: `.env` ainda no histórico git (há 14 commits com exposição da ANON key)

## Próximos passos sugeridos
- Rotacionar chave ANON do Supabase (exposta em 14+ commits)
- Adicionar `email_confirm: true` no `Register.tsx` (signUp)
- Normalizar role `'CLIENT'` para `'cliente'` no Register.tsx
- Tratar erro de criação de processo no Register.tsx (atualmente silencioso)
